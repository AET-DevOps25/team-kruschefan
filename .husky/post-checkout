set -e

# Go to root directory of the repository
cd "$(git rev-parse --show-toplevel)"

# Update openapi spec
echo "🛠️- Updating OpenAPI spec..."

./api/scripts/setup.sh

echo "✅- OpenAPI spec updated"